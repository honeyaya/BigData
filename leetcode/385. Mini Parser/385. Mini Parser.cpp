class Solution {
public:
    NestedInteger deserialize(string s) {
        NestedInteger ans;
        int len = s.length();
        
        string tmp;
        for(int i = 0; i < len; i++){
        	if(s[i] == ',') continue;
		}
    }
};
